package com.mingyoojung.codejam;

import org.junit.Test;

import static org.junit.Assert.*;

public class ReverseWordsTest {

	@Test
	public void nameTest() throws Exception {

		System.out.println(ReverseWords.reverse("entry too a the item less snow base less bar chars only a only the would than pony this a only than than this words long than than cola five word per with snow set less all too the too the would only item less alpha base"));

	}
}
